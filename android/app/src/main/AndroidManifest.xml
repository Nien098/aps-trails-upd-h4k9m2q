<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>
    <!-- Lets tracking (and the stillness safety alert) survive a locked screen
         or the app being backgrounded during a walk. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Keeps a region/app-update download running to completion when the
         app is backgrounded or the screen locks (see DownloadKeepAliveService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Emergency stillness alert (opt-in, Settings). -->
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <!-- In-app updater: lets the app launch the system installer on a
         downloaded update APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="APS Trails"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Open a shared .trail file with TrailGuide to import it.
                 We accept application/json (our real type) AND
                 application/octet-stream, because chat apps like WhatsApp don't
                 recognise the .trail extension and hand it over as a generic
                 binary (.bin, octet-stream). MainActivity validates the
                 contents, so opening the wrong file just reports "not a route". -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:mimeType="application/json"/>
                <data android:scheme="file" android:mimeType="application/json"/>
                <data android:scheme="content" android:mimeType="application/octet-stream"/>
                <data android:scheme="file" android:mimeType="application/octet-stream"/>
                <!-- Fitness history: Runkeeper CSV export and GPX tracks. -->
                <data android:scheme="content" android:mimeType="text/csv"/>
                <data android:scheme="content" android:mimeType="text/comma-separated-values"/>
                <data android:scheme="content" android:mimeType="text/plain"/>
                <data android:scheme="content" android:mimeType="application/gpx+xml"/>
                <data android:scheme="content" android:mimeType="application/xml"/>
                <data android:scheme="content" android:mimeType="text/xml"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/json"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="text/csv"/>
                <data android:mimeType="text/comma-separated-values"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/gpx+xml"/>
                <data android:mimeType="application/xml"/>
                <data android:mimeType="text/xml"/>
            </intent-filter>
        </activity>
        <!-- Minimal foreground service: its only job is to keep the app
             process alive (and thus the GPS stream, TTS, and stillness watch
             running) while the screen is off or the app is backgrounded. -->
        <service
            android:name=".TrackingService"
            android:foregroundServiceType="location"
            android:exported="false"/>
        <!-- Keeps the process alive for the duration of a region/app-update
             download — see DownloadKeepAliveService. -->
        <service
            android:name=".DownloadKeepAliveService"
            android:foregroundServiceType="dataSync"
            android:exported="false"/>
        <!-- "I'm OK" action on the stillness-nudge notification — only ever
             invoked via our own PendingIntent, never by another app. -->
        <receiver
            android:name=".StillnessAckReceiver"
            android:exported="false"/>
        <!-- Pause/Resume action on the walk-tracking notification — only
             ever invoked via our own PendingIntent, never by another app. -->
        <receiver
            android:name=".WalkControlReceiver"
            android:exported="false"/>
        <!-- Exposes the downloaded update APK (app-private storage) to the
             system package installer via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
